# ACR-004 — Activate routing scope (REQ-008, static + policy)

Status: accepted. Baseline: v1 (scope activation).
Date: 2026-09-21. Raised by: ORCHESTRATOR, decided by: OWNER (static+policy choice).

## Context
REQ-008 waited DEFERRED as phase 3. Owner now activates it with reduced
blast radius: static routes + prefix-list/route-map policy only. No
neighbors, no OSPF/BGP. Live execution stays out of scope per ACR-003;
proof remains fixtures + dry-run + tests.

## Decision
1. REQ-008 SHOULD/DEFERRED -> MUST/ACTIVE (static routes + prefix-list +
   route-map render and policy validation).
2. New INV-005: route-map prefix-list references must resolve to defined
   prefix-lists (test-verified in T-007).
3. New task T-007 (depends T-002) owns roles/routing/ + templates/ios/routing/.
4. Authorized cross-task amendments: sample gains static routing sections,
   schema documents them as optional. No other DONE files touched.
5. ASM-006 updated: routing is ACTIVE phase 3.

## Impact
Trace/gates recomputed; G-03/G-04 cover the new ACTIVE items via T-007.

## Rollback
Revert to DEFERRED, remove T-007 (only valid before it starts).
