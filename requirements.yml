# Pinned collection dependencies (T-000).
# Exact pins: the versions hosted CI installed and passed with at project
# close (2026-09-23, run 35905256762).
collections:
  - name: cisco.ios
    version: "11.6.0"
  - name: arista.eos
    version: "12.3.0"
  - name: ansible.netcommon
    version: "8.7.1"
  - name: community.general
    version: "13.4.0"
  # SR Linux JSON-RPC modules (T-012, ADR-007). Exact pin: proven on
  # SR Linux 26.7.2 during planning (config idempotency, root replace).
  - name: nokia.srlinux
    version: "1.1.1"
