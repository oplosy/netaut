COMPLETION REPORT — T-001 (intended-state model + NTP/DNS/Syslog common role)
Executor: ORCHESTRATOR (single-agent sequential run). Baseline: v1.

## Files created (all inside owns, outside must_not_touch)
- netbox/schema.yml (contract v1: sites/vrfs/vlans/prefixes/devices/common + rules)
- netbox/intended/sample.yml (LAB-DC1, MGMT VRF/vlan 99, 10.0.0.0/24, 2 devices)
- roles/common/defaults/main.yml (netaut_common: null, fail-closed default)
- roles/common/tasks/main.yml (assert + ios_ntp_global/ios_system/ios_logging_global)
- roles/common/tests/test.yml (sample-vars syntax test play)

## Verification (orchestrator rerun)
V1 schema validation -> SCHEMA OK (exit 0)
  All cross-references resolve (site/vrf/vlan/prefix), VLAN ids in range,
  CIDR + IPs parse, common server lists non-empty. A1 satisfied.
V2 role syntax-check -> exit 0 (ansible-playbook --syntax-check, WSL
  ansible-core 2.21.4). A2 satisfied.
  Collections resolved from requirements.yml: cisco.ios 11.6.0,
  ansible.netcommon 8.7.0, community.general 13.4.0 (was UNVERIFIED, now pinned
  by resolution; requirements.yml ranges hold).

## Deviation (minor, non-architectural, same class as T-000)
Spec V1 cmd points at scripts/validate_model.py, owned by T-005 and not yet
existing. Interim: equivalent rule check ran from temp dir (no repo writes
outside owns); canonical script in T-005 re-validates. Rollback cost: none.

## Invariants / risks
INV-002 preserved: sample holds desired state only, zero operational facts.
INV-003 preserved: role has no Jinja2/vendor CLI strings; vendor abstraction
via collection modules only. RISK-001 addressed: pre-assert fails closed on
missing common settings; null default cannot render half-config.

## Out of scope respected
Nothing under templates/, playbooks/, scripts/, .github/ touched. VLAN render
stays in T-002, gating in T-003.
