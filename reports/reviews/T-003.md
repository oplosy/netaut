# Independent review — T-003 (gated deploy with rollback)

Reviewer: REVIEW_AGENT (spec + files + verification output only; no executor context).
Date: 2026-09-21. Task risk: HIGH, review_required: true.

## Scope reviewed
`roles/verify/files/precheck.py`, `roles/verify/tasks/main.yml`,
`roles/verify/tests/test_precheck.py`, `playbooks/deploy/site.yml`,
`playbooks/deploy/rollback.yml`, `docs/orchestration/change-requests/ACR-001.md`,
`inventories/lab.yml` (one-line ACR amendment).

## Findings
1. Gate ordering is real: pre_tasks (load + verify role) precede all device
   tasks; verify role delegates to localhost with run_once. A conflict exits
   before any connection task is reachable. REQ-003 holds by construction.
2. `check_mode: false` on the gate task is correct and load-bearing: without
   it the gate silently skips under --check (observed during execution) and
   V2 would prove nothing. Good catch by executor, correctly recorded.
3. ACR-001 is legitimate and minimal: one-line default filter, live behavior
   unchanged, cross-task amendment authorized in writing. No scope creep.
4. Rescue path proven by drill: forced post-check failure -> rescue ->
   per-host markers written -> FAILED recorded (failed=1, rescued=1).
   No secret strings in drill output or marker content. INV-001 holds.
5. Live rollback is a fail-closed assert, not a restore. Acceptable for
   phase 1 (no live devices exist) but MUST NOT be mistaken for a tested
   production rollback. Tracked: live restore lands with T-004 backup
   integration; re-tier to CRITICAL review if prod enters scope first.

## Limitations (accepted, not blockers)
- Precheck validates intended state only, not live device state; live drift
  comparison is T-004's job (REQ-006).
- Post-check in render-only is a wiring assert; real effective-state
  comparison arrives with live waves.

## Verdict
APPROVE. Implements REQ-003/REQ-004, preserves INV-001/INV-004, addresses
RISK-001/RISK-002/OPS-001. Cleared for DONE.
