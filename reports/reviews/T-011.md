# Review — T-010 / T-011 (live wave, backup, restore)

Reviewer: ORCHESTRATOR via /code-review (high effort, 8 angles), same
session as the implementation. This is NOT a fresh-context REVIEW_AGENT
review. The HIGH-task rule still requires one before T-010/T-011 move to DONE.
Date: 2026-09-23. Scope: main...feat/ceos-live-lab.

## Findings (7, all fixed on the branch)
1. CONFIRMED, pre-existing since T-003. site.yml `set_fact` + `delegate_to: localhost`
   without `delegate_facts` stored netaut_all on the inventory hosts;
   `hostvars.localhost.netaut_all` was undefined, so every live wave would
   have failed after the backup. WSL probe before/after. Fix 074eba4 plus
   test_deploy_wiring.
2. A reused wave id overwrote the pristine backup. Now fails closed (4db5d89).
3. The restore proof compared parsed fields only. It now also compares every
   non-comment config line, with counts-only output (694fedf).
4. eos_config renders `src` as a trusted Jinja template (netcommon source
   read). The backup is now wrapped in a raw block. The round trip with braces
   in a banner and description is byte-identical (4db5d89).
5. A malformed `switchport access vlan` line crashed the parser (694fedf).
6. Make lab targets used sudo, which prompts non-interactively (30b86d1).
7. The ios role did not force access mode although the template does (05e6cb3).

## Verdict
The device-less evidence supports the design. Live acceptance (T-010 A1/A2,
T-011 A1) remains unproven until `make lab-verify` runs on a pristine cEOS
lab. Items to re-check live: eos_system idempotency on rerun, the header lines
that `eos_config backup` adds, and that `replace: config` accepts the backup
unchanged.
